export type getTenantResponse = {
    name: string,
    mainColor: string,
    secondColor: string,
}

export type TenantProps = {
    slug: string;
    name: string,
    mainColor: string,
    secondColor: string,
}